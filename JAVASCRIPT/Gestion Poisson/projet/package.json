{
  "name": "senthiof-api",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port ${PORT:-3000} --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}